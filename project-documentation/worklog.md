# Worklog

What has been done to this repository, what has been *checked*, and how long it took.
Newest entry first.

`lrd-logbook.md` does this for one document. This file does it for the project, and it
exists because of something that happened on 15 September 2026: a prose check was run
across the whole site, and 125 of its 247 findings turned out to have been reviewed and
deliberately kept nine days earlier. The reasoning was written down both times — once in
the LRD logbook, once in a skill file — and an agent starting fresh found neither. Work
that has already been judged should be findable in one place before anyone repeats it.

**Read the checks register first.** If the thing you are about to inspect is already in
it, read the verdict before you start.

## How to add to it

1. **A substantive change** — a page published, a document rewritten, a decision taken —
   gets a row in the sessions ledger.
2. **Every check that runs** gets a row in the checks register, *including the ones that
   find nothing*. A clean result is the row that saves the next person an afternoon. A
   finding you looked at and decided to keep is worth more still: write down why, because
   the check will report it again next time.
3. **The hours come from the Claude Code transcripts**, never from memory:

   ```bash
   python3 .github/scripts/worklog.py hours    # what is not yet booked
   python3 .github/scripts/worklog.py book     # record what has been counted
   python3 .github/scripts/worklog.py verify   # the header still adds up
   ```

   `hours` reads the transcripts on your own laptop and reports what is missing from this
   file. Add a row per working day and update the total, then run `book`, which writes the
   figures into the `worklog:booked` comment at the foot of this file so nothing is counted
   twice. `book` stores minutes against each session id rather than the id alone, so a
   session booked while the conversation is still open gets topped up next time instead of
   losing its tail. `verify` runs anywhere, including CI, and fails when the total above no
   longer matches the rows below.

**How the hours are measured.** Active time: the gaps between consecutive messages in a
session, each gap capped at **15 minutes**. A session's first-to-last span was the obvious
measure and it is wrong — two September sessions span eighteen and twenty-two hours because
the window stayed open overnight, which booked 41 hours against a fortnight holding about
six. The cap moves the figure (5 minutes gives 4.4 h, 30 minutes gives 8.5 h over the same
transcripts), so it is fixed at 15 and named here.

**What is not counted.** Anything done away from Claude Code — reading, teaching, marking,
thinking on the train. Transcripts begin on **13 September 2026**; the repository begins on
**2 June 2026**, and the hours of those first three months were never measured. They are
absent rather than estimated.

---

Total recorded: **14.51 h** over 3 working days, from 13 September 2026.

## Checks register

What has been inspected, when, and what was concluded. A verdict here stands until someone
supersedes it with a new row.

| Target | Check | Date | Verdict |
|---|---|---|---|
| `site/week-03.html`, `site/week-03-slides.html` | `aiprose.py` A1,A16,A17 and D1,D3; `slidewords.py`; `check-links.sh` | 20 Sep 2026 | **Four findings fixed, one kept.** Two D1 sentences over 25 words and one A17 demonstrative echo, all in the new homework assignment, were rewritten; an A1 in the footer's *educational, not consultancy* is house framing and stays, as it does on every page. The eight cards are all inside the 40-word, 3-bullet, 20-word limits. |
| `experiment_service/` | `check-service.py`, with two new SV-4 cases for the fixed retention deadline | 20 Sep 2026 | **Green, 31 passed.** The window is now an instant rather than a day (ADR-0017): a row written five days before the debrief is still there, and gone the moment the debrief evening passes. The old one-day fallback is unchanged for a store with no deadline set, which is what the local runs and the gates use. |
| `work/decisions/` | `check-adrs.sh`, and again with `BASE_SHA=origin/main` | 20 Sep 2026 | **Green, 17 records.** ADR-0017 added, ADR-0016 set to `Superseded by ADR-0017` — the status line is the only edit the guard allows on an accepted record, and it passed with the base set. |
| The deployed `experiment-service` | *Not checked.* | 20 Sep 2026 | **Open, and it is a promise the page already makes.** The running revision still purges at 19:00 nightly, so a submission sent on the Tuesday dies on the Tuesday. `deploy.sh` now carries `RETENTION_UNTIL`, but nothing is true until an owner runs it. It is the first item in the manual's must-do list. |
| `site/blueprint-criteria.html` | Read-through for the forms `aiprose.py` cannot see (A1 without a signal word, A13–A15); `aiprose.py` all codes and D1,D3 after; `check-links.sh` | 18 Sep 2026 | **Four sentences rewritten, the rest kept.** Out: the epigram closing D3 (*Silence is also an answer…*), F2's triplet title (*real, owned and routed*) and its comparison with a straw man (*worth more than a filled section that asserts*), and the bolded *beats three pages that do not*, which repeated *Brevity is not penalised* beside it. Kept, because each rejects an answer a student would plausibly write: *"Uses" or "sends" is not a label*, *"We follow the GDPR" is not a line*, *has a name but no job*, *material, never an instruction*. Kept for parallel with `prd-criteria.html`: the group title *The document as a working instrument*. The top notice now says these criteria govern the feedback and the marks use separate ones, so it no longer reads as contradicting *Where this comes from*. The script's two findings are the ones in the row below and stay for the same reasons. D1,D3 clean, links green. |
| `site/blueprint-criteria.html` | `aiprose.py`, all codes; `check-links.sh` | 18 Sep 2026 | **Two findings kept.** Four D1 sentences, one D3 term (*stakeholder*) and half the em dashes were fixed. Kept: A10 at 1.5 em dashes per 100 words, because most are the `A1 — ` item labels the PRD criteria page uses too; and A1 on the footer's *educational, not consultancy*, which is the house line. Links green, including the three pages that now point here instead of promising it. |
| `site/tool-bias-experiment.html` | `SV-9`, the interpretation set | 15 Sep 2026 | **One of four sentences was unreachable, and is gone.** The set had a region for an interval *narrower* than the smallest difference the sample could detect. That cannot happen at any sample size: a 95% interval runs about 1.4 times the width of an 80%-power detectable effect — 1.52 at n = 3, 1.41 at 32, 1.40 at 2000 — because both are fixed multiples of the same standard error. It was caught by the fixture built to reach it landing somewhere else. The set is three, the fact now sits inside the spanning sentence where it reads as arithmetic rather than as a failed session, and a check sweeps sixteen sample sizes to insist every stored sentence is reachable. |
| `site/tool-bias-experiment.html` | `SV-9`, `SV-10`, `AN-2`, `AN-3` in `check-session.mjs` | 15 Sep 2026 | **Green.** 3 fixtures, 3 different sentences, all from the stored set, 0 composed. 7 summaries missing a governing number, 0 sentences drawn — `SV-10` is held by `governingNumbers()` returning null, so a template has nothing to render into rather than rendering a hedge. An 11-phrase verdict list over the set finds nothing, and every sentence names something the data cannot do. `AN-2`: 23 figures over 24 pairs down both the live path and the paste path, equal to 3 dp — true by construction, since the dashboard reshapes rows and hands them to the same `pairUp` and `summarise` phase 5 checked against scipy. Four sabotages tried, four caught. |
| `site/tool-bias-experiment.html` + the service | Live dashboard, real browser, 26 submissions | 15 Sep 2026 | **Green.** The instructor view connected with the owners' token and drew the comparison from 24 seeded submissions. A 25th and 26th were then submitted from outside the page: **visible 4.27 seconds later**, against `SV-7`'s ten, with the poll running every five. The interval, the detectable effect and the chosen sentence all moved with them. Without `?instructor` the whole section is hidden and none of its 15 controls is focusable, so a student tabbing past the finish cannot land in the token box. At a 400 px viewport with all three fixtures loaded in turn, the document scrolls 400 px. |
| `site/tool-bias-experiment.html`, the interpretation sentences | `strip-ai-language`, A-codes | 15 Sep 2026 | **Not reachable by the tool, read by hand instead.** The three sentences are built at run time from template functions, so `aiprose.py` reads the page and never sees them. Read by hand: one A1, *that is arithmetic rather than a fault*, kept — it exists to stop a room reading a wide interval as a failed session, which is the contrast-is-content case this register has settled twice. The page's own markup is unchanged at 5 A1, all previously settled. |
| `experiment_service/` | `check-service.py` (new) | 15 Sep 2026 | **Green, after one bug of my own.** Every route driven end to end through WSGI with the clock injected, so it needs no port, no container and no cloud project and the retention check fast-forwards a day rather than waiting one. 25 checks: 18 payloads carrying something they should not, 18 refused; 5 attempts by 5 means to fetch one student's row, 0 returned one; 0 codes in any response; 4 other origins refused a CORS header. The bug: the first `/purge` accepted `X-CloudScheduler: true` as proof of the caller. That is a header, not a credential, and it let anyone wipe the room's data mid-session with one `curl`. The scheduler now carries its own token and the spoof is a check. Five sabotages tried, five caught. |
| `site/tool-bias-experiment.html` | `check-session.mjs`, rewritten for `AG-1` | 15 Sep 2026 | **Green, and the check itself was wrong before it failed.** It asserted that the file contained no network call at all. That passed for four phases and was never what `AG-1` says — ADR-0016 gave this page a submission endpoint, and an assertion that something is *absent* reads later as a requirement that it stay absent. It now enumerates every way out of the page, accounts for each, and forbids the transports this page has no business having. Added with it: `SV-1`/`SV-2` (0 requests across a full session, 1 after 1 press, 0 of the student's sentences in the body) and `SV-6` (10 of 10 sessions finish with the service refusing, 500ing, 401ing, never answering, or absent). Four sabotages tried, four caught. |
| `site/tool-bias-experiment.html` + `experiment_service/` | Real browser, real service, full session | 15 Sep 2026 | **Green.** A session driven through the page's own controls against the service on a laptop. **Three network requests in total: the page, the CORS preflight, the submission** — 0 to any other origin. The two position sentences and the pasted assistant reply appear nowhere in what the service holds, and neither does the code `kite`: the row carries a salted hash. Delete-by-code removed it; an anonymous read got 401. Then the same session pointed at a dead port: the finish was reached, the line produced, the copy button worked, and the screen said what had happened and what to do about it. |
| `site/tool-bias-experiment.html` | `check-presentation.py` (new) | 15 Sep 2026 | **Green, after one fix.** The check builds the page's document tree and runs a small CSS cascade over it, because a colour pair belongs to an element and not to a rule: every eyebrow on the page is `--text-muted`, and it only becomes a defect when it lands inside a cream box three levels up. 206 text nodes in 2 state variants, all at or above 4.5:1, 0 muted-on-cream. Token block byte-identical to `index.html`, one nav whose wordmark matches it, one footer with one credit. Every check was sabotaged and confirmed to fail, including the one that matters most: a selector the matcher cannot read is reported rather than silently skipped. |
| `site/tool-bias-experiment.html` | 400 px, measured in Chrome | 15 Sep 2026 | **One defect found and fixed.** Card `c1-05`'s APA reference ends in a 73-character USENIX path with no hyphen in it, and pushed the document to 531 px at a 400 px viewport. `.card-meta` now allows a break inside it. The static screen first accused the wrong card: the longest URL in the deck is 157 characters and wraps unaided. Measured in Chrome, of `/ - ? & # = _ . : , + ~ % @ |` only `-` and `?` are break opportunities, so the hyphen-rich Dutch URL was never the problem. With the fix, every screen unhidden at once and the widest strings the deck can produce loaded in: document scrolls 400 px, **0 elements outside the viewport**. `PR-2` and `PR-4` still want their demonstration; a headless Chrome is not a phone in a hand. |
| `site/index.html`, new tool card | `strip-ai-language`, A-codes | 15 Sep 2026 | **1 new finding, kept.** *It measures what you opened, not what you know* is the contrast-is-content case this register settled on 15 Sep: what a student will assume the tool measures is exactly the half being negated, and the sentence stops working without it. A1 on `index.html` goes 16 → 17; the tool page's own 5 are unchanged and already settled. |
| `site/tool-bias-experiment.html` | `check-measures.py` (new) | 15 Sep 2026 | **Green.** Ten measurement sessions, five classes of result lines, four kappa matrices. Every figure recomputed in Python from `work/drafts/week-03-codebook.md` and compared with the same figure produced by the shipped page's own code, which `runpage.mjs` evaluates outside a browser. The two implementations disagree in method where they can: scipy's `t.ppf` against a t quantile from the incomplete beta, and scipy's non-central t against the page's own Simpson integration of it. All matched to 3 dp. The power figure was checked a third way, against a 200,000-run simulation, which returned .8012 against a nominal .80. |
| `site/tool-bias-experiment.html` | `check-session.mjs` (new) | 15 Sep 2026 | **Green.** Whole sessions driven through the page's own code in a `node:vm` stub DOM. 20 fresh sessions gave 20 distinct orders, all 20 stored (`SM-2`, `SM-9`); the 9th open was refused 800 times out of 800 across 100 sessions (`SM-3`); the card index never fell (`SM-4`); one storage key, nothing written before consent, nothing left after erase (`DA-1`); all 8 teams met the assistant in the right round and before card 1, and none got past an empty paste box (`AG-3`); 5 cards timed to within 0 ms of the wall clock (`MS-5`). |
| `site/tool-bias-experiment.html` | `SM-8`, answer key in the shipped file | 15 Sep 2026 | **Two leaks found and fixed.** The check looks for cell codes, for card ids within 160 characters of a stance or quality word, and for a seventh field on any card record. It caught two pieces of *my own* interface text — a worked example reading `c1-01 NAS3` in a hint and in an error message, which is that card's real cell. Both replaced with a description of the format. This is the check most worth keeping: the leak was introduced by someone who knew the rule, while writing help text about the rule. |
| `site/tool-bias-experiment.html` | `check-prompt.py` (new) | 15 Sep 2026 | **Green.** The assistant prompt is built through the page's own `agentPrompt()` and run against a 48-phrase verdict list. It asks for four questions, names the claim and the locked sentence, carries none of the 48 phrases, and speaks of the position in the third person while addressing the student as *you*. The claim and the student's sentence are stripped before the list is applied: a claim contains *should* by construction. The prompt deliberately carries no list of banned words, which would put them in the prompt. |
| `site/tool-bias-experiment.html` | Real browser, full session and lecturer view | 15 Sep 2026 | **Green.** Two rounds end to end: 32 distinct cards, 8 question groups of 7 per round, unanswered questions and an empty paste box both refused, one storage key, a line that parses. **One network request across the whole session: the page itself** (`AG-1`). The lecturer view took a 15.8 KB paste of the codebook markdown and read 32 cards from it, accepted 32 lines, listed 3 malformed ones with reasons and skipped a blank without counting it, and rendered the question with both its governing numbers. `?instructor` reveals it, `?student` hides it again. |
| `site/tool-bias-experiment.html` | `strip-ai-language`, A-codes and D-codes | 15 Sep 2026 | **7 findings, 2 rewritten, 5 A1 kept.** The five kept are the contrast-is-content case this register already settled on 15 Sep: *about the round you have just finished, not about the claim* stops a student answering about the claim, *listed back rather than dropped* names the failure `AN-6` exists to prevent. Rewritten: one sentence carrying two antitheses at once, and a lecturer hint that ran to 30 words. 0 D-code findings afterwards. |
| All of `site/` | `strip-ai-language`, A-codes | 15 Sep 2026 | 247 findings, of which **125 were already closed** by the rows below and 11 are the em-dash norm. Of the 111 left, **six were rewritten** and the rest keep their contrast, which on these pages is usually the content: *informational, not scored* and *questions, not feedback* stop meaning anything without the negated half. Rewritten: the Draghi paragraph on `index.html` (it corrected a misreading nobody on the page had voiced, so it now states the positive fact), *not one after the other* and a demonstrative *That is why* on `week-01.html`, two hollow uses of *actually* on `week-02.html`, the *founding the company again* image on the blueprint tool (the sentence before it already gave the mechanism), and a restating closer on card 12 of the week-2 slides. |
| Student pages | `strip-ai-language`, D1 and D3 | 15 Sep 2026 | **Measured and left open: 68 sentences over 25 words** (26 on `index.html`, 16 on `week-01.html`, 13 on `prd-criteria.html`, 7 on `handbook.html`, 6 on `week-02.html`) and one unexplained term, *framework* in a `prd-criteria.html` list of implementation detail the reader is told to leave out. These are reader-fit rather than machine-written prose, and rewriting 68 sentences changes the course's voice, which is the module owner's call. |
| `project-documentation/slides/week-0*-notes.md` | `strip-ai-language`, A-codes | 15 Sep 2026 | **12 findings, all left.** A lecturer's spoken script for an audience of one who wrote it; every contrast mirrors a distinction taught on the page, and a demonstrative closer is ordinary emphasis in speech. |
| `site/week-02-slides.html` | `slidewords.py` | 15 Sep 2026 | **Open: 7 of 15 cards over the 40-word body limit**, card 13 also over the three-bullet limit. Pre-existing and untouched by the prose pass, which took card 12 from 55 words to 53. |
| `site/integrated-lrd.html` | `strip-ai-language`, A-codes | 6 Sep 2026 | Eight uses of *actually* and two demonstrative echoes removed. **The 120 A1 antitheses are deliberate and stay** — ruling out a wrong reading is what a specification does, and *the team's tool, not the handbook's precondition* loses its meaning without the negated half. Three uses of *actually* stay for the same reason. Reasoning in `lrd-logbook.md` v1.8. |
| The five published pages, 24,034 words | AI vocabulary word list, 42 terms | 6 Sep 2026 | **Four hits, all four fair.** The word-list method finds nothing here; the antithesis is the form this site actually produces. Recorded in `.claude/skills/strip-ai-language/SKILL.md`. |
| `site/` | `strip-ai-language` A4, A5, A7 | 6 Sep 2026 | **Five findings, all five kept.** *Regional ecosystem* is the name of a real thing, *unlock* describes a gate that is literally locked, *either way* fell mid-sentence rather than at the end. |
| `site/tool-ai-worker-maturity.html` | all prose checks | standing | **Out of scope.** Reproduced from the AI Wiki and maintained there; its footer says so. Do not edit its prose here. |
| Em-dash density (A10) across `site/` | `strip-ai-language` A10 | standing | **House style.** Every page trips it and every page is meant to. `CLAUDE.md` settles this; it does not need deciding again. |
| Every relative link in the repo | `check-links.sh` | every push and PR | Green. Runs in CI, so it needs a row here only when it finds something. |
| `work/decisions/` | `check-adrs.sh` | every push and PR touching it | Green, 15 records. |
| `build/week-03-agents-deck/` — PRD, blueprint, build plan | `strip-ai-language`, A-codes (D-codes not run: the reader is the module owners) | 18 Sep 2026 | **22 findings, 8 rewritten, 14 kept.** Kept: seven A1 where the contrast is the design — *in the note, not on the card*; *what happened in the room, not what the hypothesis predicted*; *a label rather than a claim*; *from the LRD rather than the wiki*; *introduces them rather than recalling them*; *trust, not policy*, which is the cited finding itself — plus the three footers' *educational, not consultancy*, settled house framing. Two A5 on *harness*: the domain term, and a quoted pattern. Two A6 on *marks*, the plainest verb available. Rewritten: four A1 whose negated half nobody on the page holds, three A17 closers. |
| `build/week-03-agents-deck/check-agents-deck.py` | Sabotage: every check broken once on a copy of the blueprint or a fixture deck | 18 Sep 2026 | **Every failure path caught** after two fixes. First run missed PR-3, because the skill's `JARGON` list carries no agent terms — the check now adds its own list, judged by the skill's `is_explained()`. The SL-5 miss was a sabotage sentence of 19 words, under the limit; redone at 26, caught. BD-1's pass path is untested until phase 3 produces real outputs. |
| `.claude/skills/strip-ai-language/scripts/aiprose.py` | Found while building the check above | 18 Sep 2026 | **A-patterns cross block boundaries.** The boundary marker is `\x1e`, and Python's `\s` matches `\x1e`, so `harness\s+the` fires on a headline ending *…the harness* above a bullet starting *The harness*. Not fixed in the skill — it is shared, and a fix changes every page's counts; `check-agents-deck.py` works around it by ending each block with a full stop. A fix in the skill would use `[ \t\n]` or exclude `\x1e` from `\s`. |
| `site/tool-bias-experiment.html`, the pilot audit (`AN-8`, `MS-7`) | `check-measures.py`, `check-session.mjs` | 18 Sep 2026 | **Green, and each check seen to fail.** The owners made the first session the pilot, so the page now records the seconds to a side and reports `CL-1`, `CL-5`, `CL-7` and `CL-10` as met, not met or not yet measurable. A Python reference recomputes all seven verdicts over six rooms — one leaning 90/10, one under the ten-person floor, rooms passing and failing `CL-10`, so every verdict occurs — and the stance matrix, observed and chance agreement and kappa over four pairs of codebooks, one with a card left out. The session check drives the panel through its buttons and holds the clock to the choice rather than the lock: 700 ms chosen, 2100 ms locked, 700 recorded. Three sabotages — the 70% bar loosened, the stance matrix miscounted, the clock stopped at the lock — three caught. The three interval fixtures sit where they did: their seconds come from a stream of their own. |
| `experiment_service/` | `check-service.py`, after the line went to version 2 | 18 Sep 2026 | **Green, after one slip of my own.** 19 payloads refused, the new one a version 1 submission without `pick`, refused for its version and, re-sent as version 2, for the missing field. The slip: the new case was first inserted mid-list, and the overrides below address that list by position, so four labels ran against the wrong payloads and still passed. Caught by reading the labels against what was sent; the case is now appended, with a comment saying why. |
| `site/tool-bias-experiment.html` | `strip-ai-language`, A-codes | 18 Sep 2026 | **5 A1, all previously settled.** The new audit notes raised three antitheses — *replace the card rather than argue it*, *judgements rather than counts*, *professional judgement rather than personal values*. All three rewritten; none was contrast-as-content. |
| `ai-in-business-507819`, the gate's cloud project | Read-only survey before the ADR-0015 instructions | 18 Sep 2026 | **As `DEPLOY.md` says, with three findings.** `socratic-gate` in `europe-west4`, five weekly jobs paused, one EU database, billing on, one owner (a personal account). The roster is still the two fixtures, and their reports are still in `reports/` and `owner_reports/`. The budget could not be read from this account. Findings: `DEPLOY.md` told you to write the roster with `gcloud firestore documents create`, which does not exist — corrected to the REST API, which was tried read-only and works. And `experiment_service` would share the gate's default database, with a project-wide `datastore.user` that can read `owner_reports/` — flagged to the owners, not changed. |
| `work/decisions/` | `check-adrs.sh` | 18 Sep 2026 | **Green, 16 records.** ADR-0016 accepted. Before that, the one sentence in Decision 6 that no longer matched the build was corrected while the record could still change: the interpretation is chosen by the interval against zero, and the detectable effect is shown beside it, not used to choose. |
| `socratic-gate`, deployed | `POST /run?week=2` on the two fixtures, before the roster is replaced | 18 Sep 2026 | **Green: 200 in 127 s, one attempt per team.** Weak page: sourcing, vetting and reasoning 1, movement n/a, ledger `NOT-FOUND` on the invented Van der Meer & Kowalski article — the same verdict as on 6 September. Strong page: 3/3/3/3, no flags — a point above 6 September's 2/3/2, the run-to-run movement ADR-0015 records. Gate A opens for both, the weak one on the first-report rule because Movement cannot be scored in a team's first week, which is what `instructions.txt` says. The team halves (4 and 5 questions) hold no score, no `n/3` and no ledger word in capitals; the one lower-case *confirmed* is the team's own sentence quoted back. First attempt from the laptop failed with *couldn't connect*: `DEPLOY.md` started the proxy in the background and called it on the next line. Replaced with a direct call on an identity token. |
| `project-documentation/week-03-experiment-manual.md` (new) | `strip-ai-language`, A-codes and D1; link check | 18 Sep 2026 | **14 A-findings, 8 rewritten, 6 kept.** Kept because the contrast is the instruction: *out of students' way rather than secret* (the site's own rule), *after the two rounds, not before* (the decision itself), *a code word, not your name*, *a demonstration, not a result* (the protocol's point), *the aggregates, not rows or codes* (what may be kept past 19:00), and one inside the claim sentence, which is quoted as the tool shows it. D1 was run although the reader is the owners, not students, because plain language is the standing preference; 10 long sentences, 3 split, the rest are list items. Writing it found a stale legend on the tool's finish screen — it still explained a `v1` line and had no entry for `pick` — fixed, with the presentation, session and measures checks re-run green. |
| `experiment-service`, deployed | Live test from the command line: health, preflight, submit, version refusal, anonymous and owner dashboard, delete | 18 Sep 2026 | **Five of six green, one blocker found and fixed in code.** Submit stored a v2 row; a v1 payload was refused for its version; the dashboard refused an anonymous read with 401, and for the owners returned the row with both `pick` values and without the code; delete removed it and the count went back to 0. **The blocker:** the CORS preflight came back 502 — `OPTIONS` answered 204 *with* a body, which the local server passed and Cloud Run's front end refuses. Every browser would have failed at submit and fallen back to copying the line. Fixed in `app.py`; `check-service.py` now asks the question outright and was seen to fail without the fix (26 checks). Needs one redeploy. Two more found on the way: the service would have shared the gate's database with a project-wide role that could read `owner_reports/` — now a named database `experiment` with the role conditioned on it; and `expiresAt` was a number, which Firestore's TTL ignores, so one of the three retention mechanisms did nothing — now a timestamp, tested against a stand-in client. The first deploy run failed binding the new account before IAM could see it; `deploy.sh` now retries. |
| `site/tool-bias-experiment.html` | All six checks, after `SERVICE.origin` was set | 18 Sep 2026 | **Green, after the session check stopped assuming no service.** Twelve failures on first run, all from checks written for a page that shipped with an empty address. The harness now runs the page with the address blanked unless a check asks for the published file; the published file is checked to name one https Cloud Run host and no other origin, to end a session on the submit screen with no request before the press, and to tell students on the consent screen that numbers may be sent and sentences never. |
| `experiment-service` revision 00002 + the tool | Preflight by `curl`, then a whole session in a real browser (Playwright) with the page served under `https://datadrivendecisions.github.io` and the real service behind it | 18 Sep 2026 | **Green end to end.** The first redeploy had not happened — only revision 00001 existed, built before the fix, and every 502 in the log came from it; the container was then rebuilt on its own and 00002 took all traffic with its settings intact. Preflight now 204 with the allow headers, for `/submit` and for `/dashboard` with `Authorization`; another origin gets no allow header. In the browser: the live-send consent text, the submit screen reached with 0 requests, exactly 1 `POST` on the press, the "with the room" message, 0 console errors. The row reached the owners' dashboard matching the line (team 6, assistant in round 1, the same four cards) and was deleted; count back to 0. `pick` read 0 because a script chooses a side at once. Not tested here: the instructor view's live panel in a browser, to keep the owners' credential out of the transcript — its CORS path is the preflight above, and the panel itself was driven in a browser on 15 September. |
| `site/tool-bias-experiment.html` + `experiment_service/`, the owners' link | `check-service.py`, `check-session.mjs`, all six checks, links, A-codes | 18 Sep 2026 | **Green, and the one step that matters seen to fail.** The owners asked for a dashboard with nothing to paste or type. The codebook moves into Secret Manager and arrives with the rows; the credential rides after the `#` in a personal link. `check-service.py` (29): the owners' dashboard carries the codebook, 6 other responses — submit, anonymous, wrong token, health, an unknown route with the owners' token, the preflight — carry none of it, and a service without one says `null` rather than inventing one. `check-session.mjs`: opening the link makes exactly one request, to the dashboard, with the credential decoded into the header; the address bar is cleared first; results and pilot audit fill from the service's codebook with nothing pasted; a reload goes on watching; Stop forgets; no link, no request; no codebook from the service stops with a pointer to the paste box. Removing the address-bar step failed the check, which is the one that keeps the credential off the projector. The extraction in `deploy.sh` yields the same 32 pairs `check-measures.py` reads from the codebook. A-codes unchanged at the 5 settled findings. Not yet run against the live service: it needs the codebook secret, which needs a deploy. |
| `site/week-03-slides.html`, the AI agents cards (s3–s5) | `slidewords.py` | 18 Sep 2026 | **Green after one cut.** Card 1 came in at 41 words; *Does your code or the model pick the next step?* became *Code or model: who picks the next step?*, which also mirrors the bullet's label. Final 38 / 36 / 38 words, 3 bullets each, longest sentence 15. The two placeholder cards (title, debrief) are not the segment's. |
| `site/week-03-slides.html` | `strip-ai-language`, `--block-tag li`, A1–A9 and D1, D3 | 18 Sep 2026 | **0 findings on the cards.** One A1 on the page, the footer's *educational, not consultancy*: settled house framing, read across a block boundary. D1 and D3 clean. *RAG*, *LLM Wiki* and *Fat Skills* pass PR-3 on the colon in their line; their gloss is the verb (*retrieves*, *compiles*, *act*) and the linked visualisation, which is the design the PRD asked for. |
| `project-documentation/slides/week-03-notes.md` | `strip-ai-language`, A-codes | 18 Sep 2026 | **2 findings, 1 kept.** Kept A1: *introduce it here rather than recall it*. The contrast is the instruction, conditional on the week 2 debrief. A2 *powerful* rewritten as the quotation it was (*"powerful but inert"*). |
| AI agents segment | `check-agents-deck.py --online`, `check-links.sh` | 18 Sep 2026 | **27 of 28, BD-1 red by decision.** Every Test rule passes, ST-1's five external links included. BD-1 wants the PDF and the PPTX; the owner asked for the deck in HTML only, so it is built with `--no-pdf --no-pptx`. BD-1 and BD-3 stay red until the blueprint is changed to match, in its own commit. Link gate green. |
| `site/week-03-deck.html`, s3–s5 at 1280 × 720 | Headless Chrome screenshots (SL-8) | 18 Sep 2026 | **No overflow.** Card 1's headline left *1* alone on a second line; shortened to *You have used an agent since week 1*, with *week&nbsp;1* unbreakable. The phone-width screenshot clips on the right, as week 2's does too: headless Chrome's minimum window width, not the page. |
| AI agents segment against the experiment's decks | Reading, before publication | 18 Sep 2026 | **Open, for the owners.** Claim 1 of the experiment is *run on small models it controls rather than frontier models it rents*, and source c1-07 says *model quality is rented but your brain is owned*. Card s4's headline (*The model is rented; the harness is the part you own*) and s5's data line sit on that axis. SE-2 guards the hypothesis direction only, so the checks pass. In the room the order is safe; on a public card page read before Monday it primes round 1. Recommendation: hold the card page off `main` until after the session (blueprint open question 2). |
| `provenance.md`, 24 rows | Every claim re-read on its wiki page; `check-agents-deck.py` CT-7 | 18 Sep 2026 | **Green, one correction to the PRD.** The PRD places the intern analogy in LRD Appendix A.1; A.1 is the module-identity table. The analogy is in Part 8, week 1. The row cites that. The experiment's assistant has no tools and no loop, so the s5 note calls it an assistant and not an agent, which card s3's definition would contradict. |
| `site/week-03.html` (new), the week 3 edits to `index.html` | Scan for the terms the experiment manual keeps off the page before the session — *confirmation*, *selective*, *devil*, *counter*, *bias* | 18 Sep 2026 | **Green.** Six hits, all the CSS `counter` property. The page links neither the tool nor the week 3 slides: the manual puts the tool's address on the projector, and the agents cards would reach students before the two rounds, which the notes call a treatment nobody randomised. |
| `site/week-03.html` | `strip-ai-language`, A-codes and D1, D3 | 18 Sep 2026 | **7 findings, 4 rewritten, 3 kept.** Three D1 sentences cut to 25 words or under, and one A1 (*boxes and words rather than code*) rewritten. Kept: the footer's *educational, not consultancy*, and A10 at 1.2 em dashes per 100 words, both house style. |
| `site/index.html`, the week 3 edits | `strip-ai-language`, A1 | 18 Sep 2026 | **1 new A1, kept.** *Open it in the session, not before* on the experiment card: the contrast is the instruction. |
| `site/week-03.html`, `site/index.html` at 1400 px | Headless Chrome screenshots | 18 Sep 2026 | **No layout faults.** The status strip reads *Week 3 · live*, and the nav links week 3. The next hand-in still names week 2's work, due Monday 21 September, because that is still true. |

## Sessions ledger

| Date | Who | Hours | Branch | What happened |
|---|---|---|---|---|
| 2026-09-15 | Witek | 8.11 | `worktree-prose-pass-and-worklog`, `week-03-decks-and-position-controls` | Built this worklog: the checks register, the sessions ledger, `worklog.py` and the CI job that refuses a total which stops matching its rows. Then ran the prose pass that prompted it across every page of `site/`, rewriting six constructions and recording the verdict on the rest. Rebuilt the week-2 presenter deck, PDF and PPTX from the edited card page. The pull request could not be opened: GitHub returned 502s and empty bodies from both the GraphQL and REST create paths while its reads stayed up. Then took the bias experiment tool from phase 1 to phase 5: the second round, the assistant step, the closing questions, the timings, the result line and the lecturer view that turns thirty-two of them into one paired comparison. Wrote the three checks that hold it — one of which caught an answer key I had put in my own help text. Then phases 6 to 8: the presentation check that reads the page's own CSS cascade and found a card whose URL pushed the page sideways on a phone; the experiment service, its container and its deploy script, written and tested but deliberately not deployed; and the live dashboard, whose interpretation set lost a sentence that could never have been selected. |
| 2026-09-14 | Witek | 5.22 | `collaboration-guardrails`, `week-03-*` | Put the pull-request route behind a ruleset on `main` that refuses a direct push, and wrote the merge-conflict agreement into `CLAUDE.md` and `SAMENWERKEN.md`. Designed the week-3 experiment on biased information seeking and specified the browser tool that runs it, then moved its result collection to a service and costed it. |
| 2026-09-13 | Witek | 1.18 | `main` | Published the session slides as one page of cards per slide, generated the presenter decks from those cards, and rebuilt the week-2 page around the session as the deck now runs it. Gathered everything written to build the system into `project-documentation/build/`. Landed phase 6 of the hand-in pipeline. |

<!-- worklog:booked
8cea137b:69.9
aef50e82:0.6
a470a9b9:0.8
75bf4d92:0.7
12e6427b:1.3
26636e49:311.5
37c8f1e5:45.8
6578e10f:167.8
8a19ba30:22.5
cfe0144c:1.2
277f19c6:135.3
22cf3541:1.1
9bd6a79e:1.1
680da1a9:0.9
32e9dcd6:0.9
c6b21fc3:1.2
b6650ce9:1.2
7bba867f:0.6
fc94e9a2:1.5
2bbec1a2:0.9
1f93dbcc:0.9
fd1e54ec:0.7
57e8a8d2:0.5
bc957735:50.3
55faa0fd:1.2
aa1d78dc:1.4
4f57e4a8:43.6
0e840fc5:2.6
19436f83:2.9
-->

# Copy to .env in this directory and fill in the key. .env is git-ignored:
# this repository is public, so a key committed here is a key published.
#
#   cp .env.example .env
#
# The key is the course-owned one with the spend cap (ADR-0009), never a
# personal key and never one shared with a student.
GOOGLE_API_KEY=""
